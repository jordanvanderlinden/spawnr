apiVersion: v1
kind: Service
metadata:
  name: {{ include "spawnr.fullname" . }}
  labels:
    {{- include "spawnr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "spawnr.selectorLabels" . | nindent 4 }}
